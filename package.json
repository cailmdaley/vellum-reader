{
  "name": "vellum",
  "private": true,
  "sideEffects": false,
  "scripts": {
    "postinstall": "npm --prefix ../vellum-next install",
    "build": "npm --prefix ../vellum-next run build",
    "dev": "npm --prefix ../vellum-next run dev -- --port 3200",
    "start": "npm --prefix ../vellum-next run preview -- --host 0.0.0.0 --port 3200",
    "typecheck": "npm --prefix ../vellum-next run typecheck"
  }
}
